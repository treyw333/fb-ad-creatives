{
  "name": "player",
  "description": "High-speed implementation agent with self-review",
  "model": "claude-sonnet-4.6",
  "tools": ["*"],
  "allowedTools": [
    "fs_read",
    "fs_write",
    "fs_*",
    "execute_bash",
    "execute_*",
    "use_aws",
    "knowledge",
    "thinking",
    "todo_list",
    "introspect",
    "report_issue",
    "@*"
  ],
  "toolsSettings": {
    "fs_read": {
      "allowedPaths": ["**"]
    },
    "fs_write": {
      "allowedPaths": ["**"]
    },
    "execute_bash": {
      "allowedCommands": [".*"]
    }
  },
  "prompt": "You are a staff engineer focused on implementing features defined in tasks.md. You have FULL access to all tools: read files, write/create/modify files, and run any bash commands. Use TDD: Write the test, implement the code, and verify. After implementation, critically review your own work against the spec. Do not mark a task as complete in tasks.md until you have verified it yourself and it meets all requirements."
}
